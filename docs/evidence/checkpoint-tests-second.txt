........................F............................................... [ 24%]
................F.F..........F.......................................... [ 48%]
.....................................................FF.....F..F
================================== FAILURES ===================================
_______________ test_ui_provider_switching_and_credential_flow ________________

qtbot = <pytestqt.qtbot.QtBot object at 0x000001AA45091DF0>
tmp_path = WindowsPath('C:/Users/khucv/AppData/Local/Temp/pytest-of-khucv/pytest-108/test_ui_provider_switching_and0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x000001AA450918E0>

    def test_ui_provider_switching_and_credential_flow(qtbot, tmp_path: Path, monkeypatch):
        from PySide6.QtWidgets import QMessageBox
    
        from vkdub.ui.main_window import MainWindow
    
        monkeypatch.setattr(QMessageBox, "question", lambda *args: QMessageBox.StandardButton.No)
        monkeypatch.setenv("VKDUB_DATA_DIR", str(tmp_path / "data"))
        monkeypatch.setattr("vkdub.media.process.MediaTools.detect", lambda self: None)
    
        backend = MockKeyring()
        monkeypatch.setattr(ElevenLabsKeyStore, "_get_backend", lambda self: backend)
    
        window = MainWindow()
        qtbot.addWidget(window)
        try:
            # Default is Vbee
>           assert window.project.voice.provider == "vbee"
E           AssertionError: assert 'vieneu_local' == 'vbee'
E             
E             - vbee
E             + vieneu_local

tests\test_elevenlabs.py:204: AssertionError
---------------------------- Captured Qt messages -----------------------------
QtWarningMsg: QFontDatabase: Cannot find font directory D:/ToolVideo/.venv/Lib/site-packages/PySide6/lib/fonts.
Note that Qt no longer ships fonts. Deploy some (from https://dejavu-fonts.github.io/ for example) or switch to fontconfig.
QtInfoMsg: Using Qt multimedia with FFmpeg version 7.1.3 LGPL version 2.1 or later
_________________________ test_build_speech_track_wav _________________________

tmp_path = WindowsPath('C:/Users/khucv/AppData/Local/Temp/pytest-of-khucv/pytest-108/test_build_speech_track_wav0')

    def test_build_speech_track_wav(tmp_path):
        # Create fake WAV chunk
        chunk_wav = tmp_path / "chunk.wav"
        with wave.open(str(chunk_wav), "wb") as wf:
            wf.setnchannels(1)
            wf.setsampwidth(2)
            wf.setframerate(24000)
            # 1 second of audio = 24000 samples = 48000 bytes
            wf.writeframes(b"\x00\x01" * 24000)
    
        from uuid import uuid4
    
        from vkdub.domain.voice import audio_key, digest, normalized_text
    
        settings = VoiceSettings()
        line = ScriptLine(id=str(uuid4()), start_ms=1000, end_ms=2000, text="Xin chào")
        script = ScriptDocument(lines=(line,))
        asset = VoiceAsset(
            cache_key=audio_key(line.text, settings),
            text_hash=digest(normalized_text(line.text)),
            provider=settings.provider,
            voice_id=settings.voice_id,
            speed=settings.speed,
            duration_ms=1000,
            output_path=chunk_wav,
            audio_sha256="0" * 64,
            generated_at="2026-09-04T12:00:00+00:00",
        )
        fake_vid = tmp_path / "video.mp4"
        fake_vid.write_bytes(b"vid")
        project = Project(
            video_path=fake_vid,
            video_duration_ms=5000,
            script=script,
            voice_assets={line.id: asset},
        )
        project.approve(True)
    
        out_speech = tmp_path / "speech.wav"
>       result_path = build_speech_track_wav(project, out_speech, total_duration_ms=3000)
                      ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests\test_render.py:96: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

project = Project(project_id='92c73eda-f423-431b-b10c-198b97f6a40e', video_path=WindowsPath('C:/Users/khucv/AppData/Local/Temp/p...nd_opacity=0.5, alignment=2, margin_bottom=50, margin_horizontal=30, max_width_ratio=0.85, line_spacing=1.1), masks=[])
output_path = WindowsPath('C:/Users/khucv/AppData/Local/Temp/pytest-of-khucv/pytest-108/test_build_speech_track_wav0/speech.wav')
total_duration_ms = 3000

    def build_speech_track_wav(
        project: Project,
        output_path: Path,
        total_duration_ms: int,
    ) -> Path:
        """Combine per-segment voice assets into a single synchronized 24kHz mono WAV track."""
        if project.script is None:
            raise ValueError("Chưa có kịch bản.")
        current_voices = project.current_voices()
        if len(current_voices) != len(project.script.lines):
>           raise ValueError("Chưa tạo đủ voice cho tất cả các câu kịch bản.")
E           ValueError: Chưa tạo đủ voice cho tất cả các câu kịch bản.

src\vkdub\services\audio_mix_service.py:17: ValueError
________________________ test_export_dialog_checklist _________________________

qtbot = <pytestqt.qtbot.QtBot object at 0x000001AA450E5280>
tmp_path = WindowsPath('C:/Users/khucv/AppData/Local/Temp/pytest-of-khucv/pytest-108/test_export_dialog_checklist0')

    def test_export_dialog_checklist(qtbot, tmp_path):
        dialog = ExportDialog()
        qtbot.addWidget(dialog)
    
        # Initially not ready
        assert dialog.btn_start.isEnabled() is False
    
        # Simulate ready project
        vid = tmp_path / "sample.mp4"
        vid.write_bytes(b"vid")
        chunk = tmp_path / "voice.wav"
        chunk.write_bytes(b"RIFF")
    
        from uuid import uuid4
    
        from vkdub.domain.voice import audio_key, digest, normalized_text
    
        settings = VoiceSettings()
        line = ScriptLine(id=str(uuid4()), start_ms=0, end_ms=1000, text="Chào bạn")
        script = ScriptDocument(lines=(line,))
        asset = VoiceAsset(
            cache_key=audio_key(line.text, settings),
            text_hash=digest(normalized_text(line.text)),
            provider=settings.provider,
            voice_id=settings.voice_id,
            speed=settings.speed,
            duration_ms=1000,
            output_path=chunk,
            audio_sha256="0" * 64,
            generated_at="2026-09-04T12:00:00+00:00",
        )
        project = Project(
            video_path=vid,
            video_duration_ms=5000,
            script=script,
            voice_assets={line.id: asset},
        )
        project.approve(True)
    
        dialog.update_checklist(project=project, ffmpeg_available=True)
>       assert dialog.btn_start.isEnabled() is True
E       assert False is True
E        +  where False = <built-in method isEnabled of PySide6.QtWidgets.QPushButton object at 0x000001AA450F2D40>()
E        +    where <built-in method isEnabled of PySide6.QtWidgets.QPushButton object at 0x000001AA450F2D40> = <PySide6.QtWidgets.QPushButton(0x1aa4424a3e0, name="primary") at 0x000001AA450F2D40>.isEnabled
E        +      where <PySide6.QtWidgets.QPushButton(0x1aa4424a3e0, name="primary") at 0x000001AA450F2D40> = <vkdub.ui.export_dialog.ExportDialog(0x1aa44249ca0) at 0x000001AA450F3580>.btn_start

tests\test_render.py:190: AssertionError
__________________ test_regenerate_one_line_preserves_others __________________

window = <vkdub.ui.main_window.MainWindow(0x1aa440af840) at 0x000001AA45107C40>
qtbot = <pytestqt.qtbot.QtBot object at 0x000001AA459D57C0>
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x000001AA4506DDF0>

    def test_regenerate_one_line_preserves_others(window, qtbot, monkeypatch):
        approve(window, qtbot)
        before = window.project.script
        calls = []
    
        def handler(request):
            body = json.loads(request.content)
            payload = json.loads(body["contents"][0]["parts"][0]["text"])
            assert len(payload["segments"]) == 1
            assert payload["segments"][0]["text"] == "Hello world"
            calls.append(request)
            return httpx.Response(
                200,
                json={
                    "candidates": [
                        {
                            "finishReason": "STOP",
                            "content": {
                                "parts": [
                                    {"text": '{"segments":[{"id":1,"translation":"Chào thế giới"}]}'}
                                ]
                            },
                        }
                    ]
                },
            )
    
        monkeypatch.setattr(
            "vkdub.services.credential_service.CredentialStore.get", lambda _: "test-key"
        )
        monkeypatch.setattr(
            "vkdub.ui.translation_controller.GeminiTranslationProvider",
            lambda key, recorder, progress: GeminiTranslationProvider(
                key, recorder, progress, httpx.MockTransport(handler)
            ),
        )
        assert window.translation.regenerate_line(before.lines[0].id)
        qtbot.waitUntil(lambda: window.translation.job is None)
>       assert len(calls) == 1
E       assert 0 == 1
E        +  where 0 = len([])

tests\test_review_ui.py:257: AssertionError
----------------------------- Captured log setup ------------------------------
INFO     vkdub:main_window.py:298 Sẵn sàng — VK Dub Studio 2.0.
INFO     vkdub:main_window.py:298 ffmpeg: Chưa tìm thấy — cài FFmpeg hoặc đặt đường dẫn môi trường.
INFO     vkdub:main_window.py:298 ffprobe: Chưa tìm thấy — cài FFmpeg hoặc đặt đường dẫn môi trường.
------------------------------ Captured log call ------------------------------
INFO     vkdub:main_window.py:298 Đã duyệt kịch bản 8ab9b89e6e3a.
INFO     vkdub:main_window.py:298 Đã lưu phê duyệt. Voice Engine chưa được tích hợp trong bản này.
INFO     vkdub:main_window.py:298 Đang dịch sang tiếng Việt qua Gemini…
INFO     vkdub:main_window.py:298 Không hoàn thành công việc API. Kiểm tra kết nối và quyền ghi dữ liệu cục bộ. Dữ liệu trước đó được giữ nguyên.
_______ test_translation_roundtrip_preserves_source_and_never_approves ________

source = Transcript(segments=(SubtitleSegment(id=1, start=0.2, end=1.9, text='Hello world.'), SubtitleSegment(id=2, start=2.0, ...aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa', cache_key='bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb')
tmp_path = WindowsPath('C:/Users/khucv/AppData/Local/Temp/pytest-of-khucv/pytest-108/test_translation_roundtrip_pre0')

    def test_translation_roundtrip_preserves_source_and_never_approves(source, tmp_path):
        draft = Translation(source_digest(source), ("Xin chào thế giới.", "Cảm ơn."))
        project = Project(video_path=tmp_path / "source.mp4", transcript=source, translation=draft)
        path = tmp_path / "draft.vkdub"
        save_project(project, path)
        loaded = load_project(path)
        assert loaded == project
        assert loaded.transcript == source
        assert loaded.state == "REVIEW_REQUIRED"
        data = json.loads(path.read_text(encoding="utf-8"))
>       assert data["schema_version"] == 5
E       assert 6 == 5

tests\test_translation.py:66: AssertionError
__________________ test_schema_two_migrates_without_writing ___________________

source = Transcript(segments=(SubtitleSegment(id=1, start=0.2, end=1.9, text='Hello world.'), SubtitleSegment(id=2, start=2.0, ...aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa', cache_key='bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb')
tmp_path = WindowsPath('C:/Users/khucv/AppData/Local/Temp/pytest-of-khucv/pytest-108/test_schema_two_migrates_witho0')

    def test_schema_two_migrates_without_writing(source, tmp_path):
        path = tmp_path / "old.vkdub"
        project = Project(video_path=tmp_path / "source.mp4", transcript=source)
        save_project(project, path)
        data = json.loads(path.read_text(encoding="utf-8"))
        data["schema_version"] = 2
        data["voice"] = VOICE
        del data["voice_assets"]
        del data["translation"]
        path.write_text(json.dumps(data), encoding="utf-8")
        before = path.read_bytes()
>       assert load_project(path) == project
               ^^^^^^^^^^^^^^^^^^

tests\test_translation.py:86: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

source = WindowsPath('C:/Users/khucv/AppData/Local/Temp/pytest-of-khucv/pytest-108/test_schema_two_migrates_witho0/old.vkdub')

    def load_project(source: Path) -> Project:
        if source.stat().st_size > MAX_PROJECT_BYTES:
            raise ValueError("Project quá lớn (tối đa 16 MB).")
        try:
            data = json.loads(source.read_text(encoding="utf-8-sig"))
        except (UnicodeError, json.JSONDecodeError) as exc:
            raise ValueError("Tệp .vkdub không phải JSON UTF-8 hợp lệ.") from exc
        if not isinstance(data, dict):
            raise ValueError("Nội dung project phải là một đối tượng JSON.")
        if type(data.get("schema_version")) is not int or data["schema_version"] not in (
            1,
            2,
            3,
            4,
            5,
            6,
        ):
            raise ValueError("Chưa hỗ trợ schema này. Hãy mở bằng phiên bản ứng dụng phù hợp.")
        allowed = {
            "schema_version",
            "legacy",
            "app_version",
            "project_id",
            "video_path",
            "output_directory",
            "source_language",
            "target_language",
            "transcript",
            "transcription",
            "translation",
            "script",
            "video_duration_ms",
            "subtitles",
            "subtitle_style",
            "masks",
            "voice",
            "voice_assets",
            "script_review",
            "export",
        }
        # Refuse later-phase data instead of silently discarding it on the next save.
        if "legacy" in data and data["legacy"] != {
            "voice_disabled": data.get("voice", {}).get("provider") in ("vbee", "elevenlabs")
        }:
>           raise ValueError("Metadata legacy không hợp lệ; giữ nguyên project.")
E           ValueError: Metadata legacy không hợp lệ; giữ nguyên project.

src\vkdub\services\project_service.py:151: ValueError
_______________ test_no_key_opens_manager_without_fake_success ________________

window = <vkdub.ui.main_window.MainWindow(0x1aa447f0960) at 0x000001AA45896500>

    def test_no_key_opens_manager_without_fake_success(window):
        assert not window.translation.start()
        dialog = window.translation.dialog
>       assert dialog.isVisible()
               ^^^^^^^^^^^^^^^^
E       AttributeError: 'NoneType' object has no attribute 'isVisible'

tests\test_translation_ui.py:62: AttributeError
---------------------------- Captured Qt messages -----------------------------
QtWarningMsg: This plugin does not support raise()
----------------------------- Captured log setup ------------------------------
INFO     vkdub:main_window.py:298 Sẵn sàng — VK Dub Studio 2.0.
INFO     vkdub:main_window.py:298 ffmpeg: Chưa tìm thấy — cài FFmpeg hoặc đặt đường dẫn môi trường.
INFO     vkdub:main_window.py:298 ffprobe: Chưa tìm thấy — cài FFmpeg hoặc đặt đường dẫn môi trường.
------------------------------ Captured log call ------------------------------
INFO     vkdub:main_window.py:298 Thiếu Gemini API key. Mở API & Chi phí để lưu khóa.
______ test_translates_through_real_adapter_mock_http_then_reuses_cache _______

window = <vkdub.ui.main_window.MainWindow(0x1aa440af840) at 0x000001AA45131640>
qtbot = <pytestqt.qtbot.QtBot object at 0x000001AA4586AD50>
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x000001AA4586AF30>
tmp_path = WindowsPath('C:/Users/khucv/AppData/Local/Temp/pytest-of-khucv/pytest-108/test_translates_through_real_a0')

    def test_translates_through_real_adapter_mock_http_then_reuses_cache(
        window, qtbot, monkeypatch, tmp_path
    ):
        calls = []
    
        def handler(request):
            calls.append(request)
            return httpx.Response(
                200,
                json={
                    "candidates": [
                        {
                            "finishReason": "STOP",
                            "content": {
                                "parts": [
                                    {
                                        "text": '{"segments":[{"id":1,'
                                        '"translation":"Xin chào thế giới"}]}'
                                    }
                                ]
                            },
                        }
                    ],
                    "usageMetadata": {"promptTokenCount": 120, "candidatesTokenCount": 30},
                },
            )
    
        mock_transport(monkeypatch, handler)
        source = window.project.transcript
        assert window.translation.start()
        qtbot.waitUntil(lambda: window.translation.job is None)
>       assert len(calls) == 1
E       assert 0 == 1
E        +  where 0 = len([])

tests\test_translation_ui.py:128: AssertionError
----------------------------- Captured log setup ------------------------------
INFO     vkdub:main_window.py:298 Sẵn sàng — VK Dub Studio 2.0.
INFO     vkdub:main_window.py:298 ffmpeg: Chưa tìm thấy — cài FFmpeg hoặc đặt đường dẫn môi trường.
INFO     vkdub:main_window.py:298 ffprobe: Chưa tìm thấy — cài FFmpeg hoặc đặt đường dẫn môi trường.
------------------------------ Captured log call ------------------------------
INFO     vkdub:main_window.py:298 Đang dịch sang tiếng Việt qua Gemini…
INFO     vkdub:main_window.py:298 Không hoàn thành công việc API. Kiểm tra kết nối và quyền ghi dữ liệu cục bộ. Dữ liệu trước đó được giữ nguyên.
=========================== short test summary info ===========================
FAILED tests/test_elevenlabs.py::test_ui_provider_switching_and_credential_flow
FAILED tests/test_render.py::test_build_speech_track_wav - ValueError: Chưa t...
FAILED tests/test_render.py::test_export_dialog_checklist - assert False is True
FAILED tests/test_review_ui.py::test_regenerate_one_line_preserves_others - a...
FAILED tests/test_translation.py::test_translation_roundtrip_preserves_source_and_never_approves
FAILED tests/test_translation.py::test_schema_two_migrates_without_writing - ...
FAILED tests/test_translation_ui.py::test_no_key_opens_manager_without_fake_success
FAILED tests/test_translation_ui.py::test_translates_through_real_adapter_mock_http_then_reuses_cache
!!!!!!!!!!!!!!!!!!!!!!!!!! stopping after 8 failures !!!!!!!!!!!!!!!!!!!!!!!!!!
8 failed, 200 passed in 7.06s
